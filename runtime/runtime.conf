# Tuple private Python runtime metadata. The checksum value is stored only in
# the referenced checksum file so the launcher has one integrity source.
RUNTIME_ID=python311-glibc217-x86_64
RUNTIME_VERSION=v0.1.0
PYTHON_VERSION=3.11.16
OPENSSL_VERSION=3.5.7
ARCHITECTURE=x86_64
GLIBC_BASELINE=2.17
RUNTIME_ARTIFACT=dcvobs-python-3.11-linux-x86_64-glibc217.tar.gz
SHA256_FILE=dcvobs-python-3.11-linux-x86_64-glibc217.tar.gz.sha256
ARCHIVE_ROOT=dcvobs-python-3.11-linux-x86_64-glibc217
# Empty means derive the GitHub Release URL from this clone's origin remote.
# DCVOBS_RUNTIME_URL can override the artifact location for another HTTPS host.
RUNTIME_URL=
